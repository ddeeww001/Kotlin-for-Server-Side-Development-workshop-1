# AI Log & บันทึกการเรียนรู้ Workshop #3 (Thai Citizen ID Validator)

เอกสารสรุปการเรียนรู้และ AI Log ตามรูปแบบรายวิชา Kotlin for Server-Side Development สำหรับ **Workshop #3: Thai Citizen ID Validator & Checksum Algorithm**

---

## 1. Prompt ที่ใช้ (สรุป)

- **Prompt 1 (สรุปโจทย์และการคำนวณ Checksum):** ให้ AI สรุปขั้นตอนและอัลกอริทึมการคำนวณ Check Digit (หลักที่ 13) ของเลขประจำตัวประชาชนไทย จากเลข 12 หลักแรก พร้อมทดสอบกับตัวอย่างเลขที่ถูกต้องและไม่ถูกต้อง
- **Prompt 2 (การแปลง Char เป็น Int และการแปลงเลขไทย):** สอบถามวิธีแปลงตัวอักษร `Char` เป็นตัวเลข `Int` ที่ถูกต้องใน Kotlin และแนวทางการแปลงตัวเลขไทย (`'๐'`–`'๙'`) ให้เป็นเลขอารบิก (`'0'`–`'9'`) เพื่อรองรับ Input ที่หลากหลาย
- **Prompt 3 (การจัดการ Edge Cases & Unit Test):** ปรึกษาแนวทางการเขียน Test Cases สำหรับตรวจสอบกรณีพิเศษ เช่น ค่า `null`, สตริงว่าง, ตัวอักษรที่ไม่ใช่ตัวเลข, เลขไทยล้วน และเลขไทยผสมเลขอารบิก

---

## 2. AI ตอบผิด / น่าสงสัยตรงไหน

### รายการที่ 1: การแปลงตัวอักษรเป็นตัวเลขด้วย `Char.toInt()` และความคลาดเคลื่อนของสูตร Checksum
- **ความผิดพลาด/น่าสงสัย:**
  - ในตอนแรก AI ได้ยกตัวอย่างการแปลงตัวอักษรแต่ละหลักเป็นตัวเลขโดยใช้ `.toInt()` (เช่น `char.toInt()`) ซึ่งใน Kotlin เวอร์ชันปัจจุบัน คำสั่งนี้เป็น Deprecated และจะคืนค่าเป็นรหัส **ASCII/Unicode Code Point** (เช่น ตัวอักษร `'0'` จะได้ค่า `48` แทนที่จะเป็นเลข `0`) ส่งผลให้ผลรวมผลคูณ Checksum ผิดพลาดทั้งหมด
  - นอกจากนี้ ในการคำนวณ Check Digit ขั้นสุดท้าย มีการเสนอสูตร `11 - (sum % 11)` โดยลืมคำนึงถึงกรณีที่ `sum % 11` ได้ผลลัพธ์เป็น `0` หรือ `1` ซึ่งจะทำให้ได้ Check Digit เป็น `11` หรือ `10` ซึ่งเป็นเลข 2 หลัก

### รายการที่ 2: การจัดการ Input แบบผสม (เลขไทย + อารบิก) และการขาด Guard Clause สำหรับ Null Safety
- **ความผิดพลาด/น่าสงสัย:**
  - AI แนะนำให้ใช้ Regular Expression ตรวจสอบเฉพาะเลขอารบิก หรือแนะนำฟังก์ชันแปลงที่รองรับเฉพาะสตริงที่เป็นเลขไทยทั้งชุด ทำให้ไม่รองรับกรณี Input ที่เป็นเลขไทยผสมกับเลขอารบิก (เช่น `"1234567890๑๒1"`)
  - ฟังก์ชันที่ AI เสนอในตอนแรกไม่ได้ใช้ `isNullOrBlank()` เพื่อจัดการ Null Safety ตั้งแต่ต้นฟังก์ชัน ทำให้เสี่ยงต่อการเกิด `NullPointerException` หรือทำงานผิดพลาดเมื่อรับค่า `null` หรือสตริงว่าง

---

## 3. เราตัดสินใจ / แก้อย่างไร

### รายการที่ 1: เปลี่ยนมาใช้ `Char.digitToInt()` และปรับสูตร Check Digit ให้สมบูรณ์
- เปลี่ยนจากการใช้ `.toInt()` มาเป็น **`Char.digitToInt()`** ซึ่งเป็นฟังก์ชันมาตรฐานของ Kotlin สำหรับแปลงตัวอักษรตัวเลข `'0'`–`'9'` ให้เป็นค่า `Int` `0`–`9` โดยตรงอย่างถูกต้องและปลอดภัย
- ปรับสูตรการคำนวณ Check Digit ให้ครอบคลุมทุกกรณีด้วยการเพิ่ม `% 10` ต่อท้าย:
  ```kotlin
  val expectedCheckDigit = (11 - (sum % 11)) % 10
  ```
- วนลูปคำนวณผลรวมน้ำหนักด้วย `for (i in 0..11)` โดยกำหนดค่าน้ำหนัก `val weight = 13 - i`

### รายการที่ 2: เพิ่มกระบวนการ Normalize เลขไทย และใส่ Guard Clause ที่รัดกุม
- เพิ่ม **Guard Clause** ที่บรรทัดแรกของฟังก์ชันเพื่อดักจับกรณี `null` หรือว่างเปล่า:
  ```kotlin
  if (id.isNullOrBlank()) return false
  ```
- ทำการ **Normalize ข้อมูล** โดยแปลงตัวเลขไทยทุกตัวให้เป็นเลขอารบิกทีละตัวอักษรด้วย `.map { ... }` ก่อนเริ่มกระบวนการตรวจสอบ:
  ```kotlin
  val normalizedId = id.map { char ->
      when (char) {
          in '๐'..'๙' -> (char - '๐' + '0'.code).toChar()
          else -> char
      }
  }.joinToString("")
  ```
- ตรวจสอบความยาวและชนิดข้อมูลให้แน่ชัดก่อนคำนวณ: `if (normalizedId.length != 13 || !normalizedId.all { it.isDigit() }) return false`

---

## 4. สิ่งที่ได้เรียนรู้

1. **ความแตกต่างระหว่าง `Char.digitToInt()` กับ `Char.code` / `Char.toInt()`:**
   - การแปลง `Char` ใน Kotlin ต้องใช้ `digitToInt()` เมื่อต้องการค่าตัวเลขจริง ไม่ควรใช้ `.code` หรือ `.toInt()` เพราะจะได้ค่า ASCII Code Point ซึ่งนำไปสู่ Logic Error ที่ตรวจจับได้ยาก
2. **การทำ Data Normalization ก่อนประมวลผล:**
   - การแปลง Input หลากหลายรูปแบบ (เช่น เลขไทยและเลขอารบิก) ให้อยู่ในรูปแบบมาตรฐานเดียวกัน (Canonical/Normalized Form) ตั้งแต่ต้นทาง ช่วยลดความซับซ้อนของ Logic และป้องกันข้อผิดพลาดในการตรวจสอบ
3. **การออกแบบและป้องกัน Null Safety ด้วย Guard Clauses:**
   - การใช้ `id.isNullOrBlank()` ร่วมกับ Early Return ช่วยให้โค้ดส่วนที่เหลือมั่นใจได้ว่าตัวแปรมีค่าแน่นอนและปลอดภัยจากการเกิด Exception
4. **ความสำคัญของการเขียน Unit Test ครอบคลุม Edge Cases:**
   - การทดสอบไม่ใช่เพียงแค่ตรวจ Checksum ที่ถูกต้อง/ไม่ถูกต้อง แต่ต้องครอบคลุมกรณีสตริงที่มีตัวอักษรปน, ช่องว่าง, ความยาวไม่ครบ 13 หลัก, เลขไทยล้วน และเลขไทยผสมเลขอารบิก

---
